<com.xywy.askforexpert.widget.SquareRelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content" >

    <ImageView
        android:id="@+id/photo_wall_item_photo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:contentDescription="@null"
        android:scaleType="centerCrop"
        android:src="@drawable/img_default_bg" />

    <!-- <CheckBox -->
    <!-- android:id="@+id/photo_wall_item_cb" -->
    <!-- android:layout_width="wrap_content" -->
    <!-- android:layout_height="wrap_content" -->
    <!-- android:layout_margin="2dp" -->
    <!-- android:layout_alignParentTop="true" -->
    <!-- android:layout_alignParentRight="true" -->
    <!-- android:button="@null" -->
    <!-- android:background="@drawable/selector_checkbox" -->
    <!-- /> -->

    <ImageView
        android:id="@+id/isselected"
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:background="@drawable/checkbox_normal"
        android:layout_margin="3dp" />

</com.xywy.askforexpert.widget.SquareRelativeLayout>